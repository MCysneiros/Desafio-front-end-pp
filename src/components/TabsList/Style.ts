import styled from '@emotion/styled';

export const Container = styled.div`
	.opaque {
		filter: opacity(25%);
	}

	.pagination {
		margin-top: 10px;
	}
	.rolePagination {
		margin-top: 10px;
		display: flex;
		justify-content: right;
	}
`;
