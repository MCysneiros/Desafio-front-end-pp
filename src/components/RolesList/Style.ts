import styled from '@emotion/styled';

export const Container = styled.div`
	display: flex;

	flex-direction: column;

	h3 {
		margin: 40px 0 20px;
		color: var(--text-dark);
		font-size: 1rem;
	}
svg{
	cursor: pointer;
}

	}
`;
